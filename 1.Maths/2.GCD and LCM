//Eucledian algorithm
//T.C=O(lon(min(a,b)))
int gcd(int a,int b){
    if(b == 0)
        return a;
    else
        return gcd(b,a%b);
}

int LCM(int a,int b){
return ((a*b)/gcd(a,b)); //a*b =gcd(a,b)*lcm(a,b)
}
